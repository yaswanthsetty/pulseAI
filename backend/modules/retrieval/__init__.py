"""Retrieval module — Phase 2 (FR-11..FR-13).

Semantic search over Qdrant (BGE-small dense vectors, cosine) is already
available as ``POST /api/v1/search``. Hybrid dense+sparse retrieval,
cross-encoder reranking, and temporal ranking land in Phase 4.
"""
